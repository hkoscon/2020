image: node:10

cache:
  paths:
    - node_modules/

install:
  stage: build
  script:
    - yarn install --frozen-lockfile

lint:
  stage: test
  script:
    - yarn lint

pages:
  stage: deploy
  script:
    - yarn generate
    - bash script/deploy.sh
  artifacts:
    paths:
      - public
  only:
    - master
